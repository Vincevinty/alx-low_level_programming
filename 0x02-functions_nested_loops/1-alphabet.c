#include "main.h"
/**
*print_alphabet - Prints the alphabet in lowercase.
*
* Return Always 0 (Success)
*/
void print_alphabet(void)
{
	print alphabet()
	return (0);
	int i;

	for (i = 97; i <= 127; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
